<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
    <!-- SQL Logging -->
    <logger name="com.example.demo.mapper" level="DEBUG"/> <!-- 修改为你的mapper包名 -->
    <logger name="org.mybatis" level="DEBUG"/> <!-- MyBatis日志 -->
    <logger name="java.sql.PreparedStatement" level="DEBUG"/> <!-- SQL语句日志 -->
    <logger name="java.sql.ResultSet" level="DEBUG"/> <!-- ResultSet日志 -->
    <logger name="java.sql.Connection" level="DEBUG"/> <!-- Connection日志 -->

</configuration>