<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- =======通过ins-framework-mybatis工具自动生成，请勿手工修改！======= -->
<!-- =======本配置文件中定义的节点可在自定义配置文件中直接使用！       ======= -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<mapper namespace="com.lawfirm.lawfirmserver.lawyer.dao.LawyerDao">
    <!-- 通用查询结果对象-->
    <resultMap id="BaseResultMap" type="com.lawfirm.lawfirmserver.lawyer.po.Lawyer">
        <id column="id" property="id"/>
		<result column="lawyerName" property="lawyerName"/>
        <result column="lawFirm" property="lawFirm"/>
        <result column="lawyerLicenseNumber" property="lawyerLicenseNumber"/>
		<result column="provinceCode" property="provinceCode"/>
		<result column="cityCode" property="cityCode"/>
		<result column="districtCode" property="districtCode"/>
		<result column="lawyerIntroduction" property="lawyerIntroduction"/>
		<result column="lawyerDetails" property="lawyerDetails"/>
        <result column="isValidFlag" property="isValidFlag"/>
        <result column="insertTimeForHis" property="insertTimeForHis"/>
        <result column="operateTimeForHis" property="operateTimeForHis"/>
    </resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id
		, lawyerName, lawFirm, lawyerLicenseNumber, provinceCode,
		 cityCode, districtCode, lawyerIntroduction, lawyerDetails, isValidFlag,
		 insertTimeForHis, operateTimeForHis
    </sql>

    <!-- 按对象查询记录的WHERE部分 -->
    <sql id="Base_Select_By_Entity_Where">
        <if test="id != null">
            and id = #{id}
        </if>
		<if test="lawyerName != null">
			and lawyerName = #{lawyerName}
		</if>
        <if test="lawFirm != null">
            and lawFirm = #{lawFirm}
        </if>
        <if test="lawyerLicenseNumber != null">
            and lawyerLicenseNumber = #{lawyerLicenseNumber}
        </if>
		<if test="provinceCode != null">
			and provinceCode = #{provinceCode}
		</if>
		<if test="cityCode != null">
			and cityCode = #{cityCode}
		</if>
		<if test="districtCode != null">
			and districtCode = #{districtCode}
		</if>
		<if test="lawyerIntroduction != null">
			and lawyerIntroduction = #{lawyerIntroduction}
		</if>
		<if test="lawyerDetails != null">
			and lawyerDetails = #{lawyerDetails}
        </if>
        <if test="isValidFlag != null">
            and isValidFlag = #{isValidFlag}
        </if>
        <if test="insertTimeForHis != null">
            and insertTimeForHis = #{insertTimeForHis}
        </if>
        <if test="operateTimeForHis != null">
            and operateTimeForHis = #{operateTimeForHis}
        </if>
    </sql>

    <!-- 按对象查询记录的SQL部分 -->
    <sql id="Base_Select_By_Entity">
        select
        <include refid="Base_Column_List"/>
        from lawyer
        <where>
            <include refid="Base_Select_By_Entity_Where"/>
        </where>
    </sql>

    <!-- 按主键查询一条记录 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from lawyer
        where id = #{param1}
    </select>

    <!-- 按主键List查询多条记录 -->
    <select id="selectBatchByPrimaryKeys" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from lawyer
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <!-- 按对象查询一页记录（多条记录） -->
    <select id="selectPage" resultMap="BaseResultMap" parameterType="com.lawfirm.lawfirmserver.lawyer.po.Lawyer">
        <include refid="Base_Select_By_Entity"/>
    </select>

    <!-- 按主键删除一条记录 -->
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete
        from lawyer
        where id = #{param1}
    </delete>

    <!-- 按主键List删除多条记录 -->
    <delete id="deleteBatchByPrimaryKeys" parameterType="map">
        delete from lawyer
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 完整插入一条记录-->
    <insert id="insert" parameterType="com.lawfirm.lawfirmserver.lawyer.po.Lawyer">
		insert into lawyer (id, lawyerName, lawFirm, lawyerLicenseNumber, provinceCode,
							cityCode, districtCode, lawyerIntroduction, lawyerDetails, isValidFlag,
                            operateTimeForHis)
		values (#{id}, #{lawyerName}, #{lawFirm}, #{lawyerLicenseNumber}, #{provinceCode},
				#{cityCode}, #{districtCode}, #{lawyerIntroduction}, #{lawyerDetails}, #{isValidFlag},
                now())
    </insert>

    <!-- 插入一条记录(为空的字段不操作) -->
    <insert id="insertSelective" parameterType="com.lawfirm.lawfirmserver.lawyer.po.Lawyer">
        insert into lawyer
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
			</if>
			<if test="lawyerName != null">
				lawyerName,
            </if>
            <if test="lawFirm != null">
                lawFirm,
            </if>
            <if test="lawyerLicenseNumber != null">
                lawyerLicenseNumber,
            </if>
			<if test="provinceCode != null">
				provinceCode,
			</if>
			<if test="cityCode != null">
				cityCode,
			</if>
			<if test="districtCode != null">
				districtCode,
			</if>
			<if test="lawyerIntroduction != null">
				lawyerIntroduction,
			</if>
			<if test="lawyerDetails != null">
				lawyerDetails,
            </if>
            <if test="isValidFlag != null">
                isValidFlag,
            </if>
            operateTimeForHis,
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
			</if>
			<if test="lawyerName != null">
				#{lawyerName},
            </if>
            <if test="lawFirm != null">
                #{lawFirm},
            </if>
            <if test="lawyerLicenseNumber != null">
                #{lawyerLicenseNumber},
            </if>
			<if test="provinceCode != null">
				#{provinceCode},
			</if>
			<if test="cityCode != null">
				#{cityCode},
			</if>
			<if test="districtCode != null">
				#{districtCode},
			</if>
			<if test="lawyerIntroduction != null">
				#{lawyerIntroduction},
			</if>
			<if test="lawyerDetails != null">
				#{lawyerDetails},
            </if>
            <if test="isValidFlag != null">
                #{isValidFlag},
            </if>
            now(),
        </trim>
    </insert>

    <!-- 更新一条记录(为空的字段不操作) -->
    <update id="updateSelectiveByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.lawyer.po.Lawyer">
        update lawyer
        <set>
			<if test="lawyerName != null">
				lawyerName=#{lawyerName},
			</if>
            <if test="lawFirm != null">
                lawFirm=#{lawFirm},
            </if>
            <if test="lawyerLicenseNumber != null">
                lawyerLicenseNumber=#{lawyerLicenseNumber},
            </if>
			<if test="provinceCode != null">
				provinceCode=#{provinceCode},
			</if>
			<if test="cityCode != null">
				cityCode=#{cityCode},
			</if>
			<if test="districtCode != null">
				districtCode=#{districtCode},
			</if>
			<if test="lawyerIntroduction != null">
				lawyerIntroduction=#{lawyerIntroduction},
			</if>
			<if test="lawyerDetails != null">
				lawyerDetails=#{lawyerDetails},
            </if>
            <if test="isValidFlag != null">
                isValidFlag=#{isValidFlag},
            </if>
            operateTimeForHis=now(),
        </set>
        where id = #{id}
    </update>

    <!-- 完整更新一条记录 -->
    <update id="updateByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.lawyer.po.Lawyer">
        update lawyer
		set lawyerName=#{lawyerName},
			lawFirm=#{lawFirm},
            lawyerLicenseNumber=#{lawyerLicenseNumber},
			provinceCode=#{provinceCode},
			cityCode=#{cityCode},
			districtCode=#{districtCode},
			lawyerIntroduction=#{lawyerIntroduction},
			lawyerDetails=#{lawyerDetails},
            isValidFlag=#{isValidFlag},
            operateTimeForHis=now()
        where id = #{id}
    </update>

</mapper>