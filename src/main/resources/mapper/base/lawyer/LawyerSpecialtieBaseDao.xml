<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- =======通过ins-framework-mybatis工具自动生成，请勿手工修改！======= -->
<!-- =======本配置文件中定义的节点可在自定义配置文件中直接使用！       ======= -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<mapper namespace="com.lawfirm.lawfirmserver.lawyer.dao.LawyerSpecialtieDao">
    <!-- 通用查询结果对象-->
    <resultMap id="BaseResultMap" type="com.lawfirm.lawfirmserver.lawyer.po.LawyerSpecialtie">
        <id column="id" property="id"/>
        <result column="specialty_name" property="specialtyName"/>
        <result column="description" property="description"/>
        <result column="insert_time_for_his" property="insertTimeForHis"/>
        <result column="operate_time_for_his" property="operateTimeForHis"/>
    </resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id
        , specialty_name AS specialtyName, description, insert_time_for_his AS insertTimeForHis, operate_time_for_his AS operateTimeForHis

    </sql>

    <!-- 按对象查询记录的WHERE部分 -->
    <sql id="Base_Select_By_Entity_Where">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="specialtyName != null">
            and specialty_name = #{specialtyName}
        </if>
        <if test="description != null">
            and description = #{description}
        </if>
        <if test="insertTimeForHis != null">
            and insert_time_for_his = #{insertTimeForHis}
        </if>
        <if test="operateTimeForHis != null">
            and operate_time_for_his = #{operateTimeForHis}
        </if>
    </sql>

    <!-- 按对象查询记录的SQL部分 -->
    <sql id="Base_Select_By_Entity">
        select
        <include refid="Base_Column_List"/>
        from lawyer_specialties
        <where>
            <include refid="Base_Select_By_Entity_Where"/>
        </where>
    </sql>

    <!-- 按主键查询一条记录 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from lawyer_specialties
        where id = #{param1}
    </select>

    <!-- 按主键List查询多条记录 -->
    <select id="selectBatchByPrimaryKeys" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from lawyer_specialties
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <!-- 按对象查询一页记录（多条记录） -->
    <select id="selectPage" resultMap="BaseResultMap"
            parameterType="com.lawfirm.lawfirmserver.lawyer.po.LawyerSpecialtie">
        <include refid="Base_Select_By_Entity"/>
    </select>

    <!-- 按主键删除一条记录 -->
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete
        from lawyer_specialties
        where id = #{param1}
    </delete>

    <!-- 按主键List删除多条记录 -->
    <delete id="deleteBatchByPrimaryKeys" parameterType="map">
        delete from lawyer_specialties
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 完整插入一条记录-->
    <insert id="insert" parameterType="com.lawfirm.lawfirmserver.lawyer.po.LawyerSpecialtie">
        insert into lawyer_specialties (id, specialty_name, description, operate_time_for_his)
        values (#{id}, #{specialtyName}, #{description}, now())
    </insert>

    <!-- 插入一条记录(为空的字段不操作) -->
    <insert id="insertSelective" parameterType="com.lawfirm.lawfirmserver.lawyer.po.LawyerSpecialtie">
        insert into lawyer_specialties
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="specialtyName != null">
                specialty_name,
            </if>
            <if test="description != null">
                description,
            </if>
            operate_time_for_his,
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
            </if>
            <if test="specialtyName != null">
                #{specialtyName},
            </if>
            <if test="description != null">
                #{description},
            </if>
            now(),
        </trim>
    </insert>

    <!-- 更新一条记录(为空的字段不操作) -->
    <update id="updateSelectiveByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.lawyer.po.LawyerSpecialtie">
        update lawyer_specialties
        <set>
            <if test="specialtyName != null">
                specialty_name=#{specialtyName},
            </if>
            <if test="description != null">
                description=#{description},
            </if>
            operate_time_for_his=now(),
        </set>
        where id = #{id}
    </update>

    <!-- 完整更新一条记录 -->
    <update id="updateByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.lawyer.po.LawyerSpecialtie">
        update lawyer_specialties
        set specialty_name=#{specialtyName},
            description=#{description},
            operate_time_for_his=now()
        where id = #{id}
    </update>

</mapper>