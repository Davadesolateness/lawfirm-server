<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- =======通过ins-framework-mybatis工具自动生成，请勿手工修改！======= -->
<!-- =======本配置文件中定义的节点可在自定义配置文件中直接使用！       ======= -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<mapper namespace="com.lawfirm.lawfirmserver.user.dao.UserDao">
    <!-- 通用查询结果对象-->
    <resultMap id="BaseResultMap" type="com.lawfirm.lawfirmserver.user.po.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="password_update_time" property="passwordUpdateTime"/>
        <result column="email" property="email"/>
        <result column="phone_number" property="phoneNumber"/>
        <result column="user_type" property="userType"/>
        <result column="source_type" property="sourceType"/>
        <result column="related_entity_id" property="relatedEntityId"/>
        <result column="is_valid_flag" property="isValidFlag"/>
        <result column="insert_time" property="insertTime"/>
        <result column="operate_time" property="operateTime"/>
    </resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id
        , username, password, password_update_time AS passwordUpdateTime, email,
		 phone_number AS phoneNumber, user_type AS userType, source_type AS sourceType, related_entity_id AS relatedEntityId, is_valid_flag AS isValidFlag,
		 insert_time AS insertTime, operate_time AS operateTime
    </sql>

    <!-- 按对象查询记录的WHERE部分 -->
    <sql id="Base_Select_By_Entity_Where">
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="username != null">
            and username = #{username}
        </if>
        <if test="password != null">
            and password = #{password}
        </if>
        <if test="passwordUpdateTime != null">
            and password_update_time = #{passwordUpdateTime}
        </if>
        <if test="email != null">
            and email = #{email}
        </if>
        <if test="phoneNumber != null">
            and phone_number = #{phoneNumber}
        </if>
        <if test="userType != null">
            and user_type = #{userType}
        </if>
        <if test="sourceType != null">
            and source_type = #{sourceType}
        </if>
        <if test="relatedEntityId != null">
            and related_entity_id = #{relatedEntityId}
        </if>
        <if test="isValidFlag != null">
            and is_valid_flag = #{isValidFlag}
        </if>
        <if test="insertTime != null">
            and insert_time = #{insertTime}
        </if>
        <if test="operateTime != null">
            and operate_time = #{operateTime}
        </if>
    </sql>

    <!-- 按对象查询记录的SQL部分 -->
    <sql id="Base_Select_By_Entity">
        select
        <include refid="Base_Column_List"/>
        from user
        <where>
            <include refid="Base_Select_By_Entity_Where"/>
        </where>
    </sql>

    <!-- 按主键查询一条记录 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from user
        where id = #{param1}
    </select>

    <!-- 按主键List查询多条记录 -->
    <select id="selectBatchByPrimaryKeys" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from user
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <!-- 按对象查询一页记录（多条记录） -->
    <select id="selectPage" resultMap="BaseResultMap" parameterType="com.lawfirm.lawfirmserver.user.po.User">
        <include refid="Base_Select_By_Entity"/>
    </select>

    <!-- 按主键删除一条记录 -->
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete
        from user
        where id = #{param1}
    </delete>

    <!-- 按主键List删除多条记录 -->
    <delete id="deleteBatchByPrimaryKeys" parameterType="map">
        delete from user
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 完整插入一条记录-->
    <insert id="insert" parameterType="com.lawfirm.lawfirmserver.user.po.User">
        insert into user (id, username, password, password_update_time, email,
                          phone_number, user_type, source_type, related_entity_id, is_valid_flag,
                          insert_time, operate_time)
        values (#{id}, #{username}, #{password}, #{passwordUpdateTime}, #{email},
                #{phoneNumber}, #{userType}, #{sourceType}, #{relatedEntityId}, #{isValidFlag},
                #{insertTime}, #{operateTime})
    </insert>

    <!-- 插入一条记录(为空的字段不操作) -->
    <insert id="insertSelective" parameterType="com.lawfirm.lawfirmserver.user.po.User">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="username != null">
                username,
            </if>
            <if test="password != null">
                password,
            </if>
            <if test="passwordUpdateTime != null">
                password_update_time,
            </if>
            <if test="email != null">
                email,
            </if>
            <if test="phoneNumber != null">
                phone_number,
            </if>
            <if test="userType != null">
                user_type,
            </if>
            <if test="sourceType != null">
                source_type,
            </if>
            <if test="relatedEntityId != null">
                related_entity_id,
            </if>
            <if test="isValidFlag != null">
                is_valid_flag,
            </if>
            <if test="insertTime != null">
                insert_time,
            </if>
            <if test="operateTime != null">
                operate_time,
            </if>
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
            </if>
            <if test="username != null">
                #{username},
            </if>
            <if test="password != null">
                #{password},
            </if>
            <if test="passwordUpdateTime != null">
                #{passwordUpdateTime},
            </if>
            <if test="email != null">
                #{email},
            </if>
            <if test="phoneNumber != null">
                #{phoneNumber},
            </if>
            <if test="userType != null">
                #{userType},
            </if>
            <if test="sourceType != null">
                #{sourceType},
            </if>
            <if test="relatedEntityId != null">
                #{relatedEntityId},
            </if>
            <if test="isValidFlag != null">
                #{isValidFlag},
            </if>
            <if test="insertTime != null">
                #{insertTime},
            </if>
            <if test="operateTime != null">
                #{operateTime},
            </if>
        </trim>
    </insert>

    <!-- 更新一条记录(为空的字段不操作) -->
    <update id="updateSelectiveByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.user.po.User">
        update user
        <set>
            <if test="username != null">
                username=#{username},
            </if>
            <if test="password != null">
                password=#{password},
            </if>
            <if test="passwordUpdateTime != null">
                password_update_time=#{passwordUpdateTime},
            </if>
            <if test="email != null">
                email=#{email},
            </if>
            <if test="phoneNumber != null">
                phone_number=#{phoneNumber},
            </if>
            <if test="userType != null">
                user_type=#{userType},
            </if>
            <if test="sourceType != null">
                source_type=#{sourceType},
            </if>
            <if test="relatedEntityId != null">
                related_entity_id=#{relatedEntityId},
            </if>
            <if test="isValidFlag != null">
                is_valid_flag=#{isValidFlag},
            </if>
            <if test="insertTime != null">
                insert_time=#{insertTime},
            </if>
            <if test="operateTime != null">
                operate_time=#{operateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!-- 完整更新一条记录 -->
    <update id="updateByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.user.po.User">
        update user
        set username=#{username},
            password=#{password},
            password_update_time=#{passwordUpdateTime},
            email=#{email},
            phone_number=#{phoneNumber},
            user_type=#{userType},
            source_type=#{sourceType},
            related_entity_id=#{relatedEntityId},
            is_valid_flag=#{isValidFlag},
            insert_time=#{insertTime},
            operate_time=#{operateTime}
        where id = #{id}
    </update>

</mapper>