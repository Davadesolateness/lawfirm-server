<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- =======通过ins-framework-mybatis工具自动生成，请勿手工修改！======= -->
<!-- =======本配置文件中定义的节点可在自定义配置文件中直接使用！       ======= -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<mapper namespace="com.lawfirm.lawfirmserver.order.dao.WelfareDistributionCustomerDao">
    <!-- 通用查询结果对象-->
    <resultMap id="BaseResultMap" type="com.lawfirm.lawfirmserver.order.po.WelfareDistributionCustomer">
        <id column="distributionId" property="distributionId"/>
        <result column="orderId" property="orderId"/>
        <result column="userId" property="userId"/>
        <result column="insertTimeForHis" property="insertTimeForHis"/>
        <result column="operateTimeForHis" property="operateTimeForHis"/>
    </resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        distributionId
        , orderId, userId, insertTimeForHis, operateTimeForHis

    </sql>

    <!-- 按对象查询记录的WHERE部分 -->
    <sql id="Base_Select_By_Entity_Where">
        <if test="distributionId != null">
            and distributionId = #{distributionId}
        </if>
        <if test="orderId != null">
            and orderId = #{orderId}
        </if>
        <if test="userId != null">
            and userId = #{userId}
        </if>
        <if test="insertTimeForHis != null">
            and insertTimeForHis = #{insertTimeForHis}
        </if>
        <if test="operateTimeForHis != null">
            and operateTimeForHis = #{operateTimeForHis}
        </if>
    </sql>

    <!-- 按对象查询记录的SQL部分 -->
    <sql id="Base_Select_By_Entity">
        select
        <include refid="Base_Column_List"/>
        from welfaredistributioncustomer
        <where>
            <include refid="Base_Select_By_Entity_Where"/>
        </where>
    </sql>

    <!-- 按主键查询一条记录 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from welfaredistributioncustomer
        where distributionId = #{param1}
    </select>

    <!-- 按主键List查询多条记录 -->
    <select id="selectBatchByPrimaryKeys" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from welfaredistributioncustomer
        where distributionId in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <!-- 按对象查询一页记录（多条记录） -->
    <select id="selectPage" resultMap="BaseResultMap"
            parameterType="com.lawfirm.lawfirmserver.order.po.WelfareDistributionCustomer">
        <include refid="Base_Select_By_Entity"/>
    </select>

    <!-- 按主键删除一条记录 -->
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete
        from welfaredistributioncustomer
        where distributionId = #{param1}
    </delete>

    <!-- 按主键List删除多条记录 -->
    <delete id="deleteBatchByPrimaryKeys" parameterType="map">
        delete from welfaredistributioncustomer
        where distributionId in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 完整插入一条记录-->
    <insert id="insert" parameterType="com.lawfirm.lawfirmserver.order.po.WelfareDistributionCustomer">
        insert into welfaredistributioncustomer (distributionId, orderId, userId, operateTimeForHis)
        values (#{distributionId}, #{orderId}, #{userId}, now())
    </insert>

    <!-- 插入一条记录(为空的字段不操作) -->
    <insert id="insertSelective" parameterType="com.lawfirm.lawfirmserver.order.po.WelfareDistributionCustomer">
        insert into welfaredistributioncustomer
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="distributionId != null">
                distributionId,
            </if>
            <if test="orderId != null">
                orderId,
            </if>
            <if test="userId != null">
                userId,
            </if>
            operateTimeForHis,
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="distributionId != null">
                #{distributionId},
            </if>
            <if test="orderId != null">
                #{orderId},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
            now(),
        </trim>
    </insert>

    <!-- 更新一条记录(为空的字段不操作) -->
    <update id="updateSelectiveByPrimaryKey"
            parameterType="com.lawfirm.lawfirmserver.order.po.WelfareDistributionCustomer">
        update welfaredistributioncustomer
        <set>
            <if test="orderId != null">
                orderId=#{orderId},
            </if>
            <if test="userId != null">
                userId=#{userId},
            </if>
            operateTimeForHis=now(),
        </set>
        where distributionId = #{distributionId}
    </update>

    <!-- 完整更新一条记录 -->
    <update id="updateByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.order.po.WelfareDistributionCustomer">
        update welfaredistributioncustomer
        set orderId=#{orderId},
            userId=#{userId},
            operateTimeForHis=now()
        where distributionId = #{distributionId}
    </update>

</mapper>