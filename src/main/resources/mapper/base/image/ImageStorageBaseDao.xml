<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- =======通过ins-framework-mybatis工具自动生成，请勿手工修改！======= -->
<!-- =======本配置文件中定义的节点可在自定义配置文件中直接使用！       ======= -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<mapper namespace="com.lawfirm.lawfirmserver.image.dao.ImageStorageDao">
    <!-- 通用查询结果对象-->
    <resultMap id="BaseResultMap" type="com.lawfirm.lawfirmserver.image.po.ImageStorage">
        <id column="imageId" property="imageId"/>
        <result column="userId" property="userId"/>
        <result column="imageType" property="imageType"/>
        <result column="fileExtension" property="fileExtension"/>
        <result column="imageData" property="imageData"/>
        <result column="insertTimeForHis" property="insertTimeForHis"/>
        <result column="operateTimeForHis" property="operateTimeForHis"/>
    </resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        imageId
        , userId, imageType, fileExtension, imageData,
		 insertTimeForHis, operateTimeForHis
    </sql>

    <!-- 按对象查询记录的WHERE部分 -->
    <sql id="Base_Select_By_Entity_Where">
        <if test="imageId != null">
            and imageId = #{imageId}
        </if>
        <if test="userId != null">
            and userId = #{userId}
        </if>
        <if test="imageType != null">
            and imageType = #{imageType}
        </if>
        <if test="fileExtension != null">
            and fileExtension = #{fileExtension}
        </if>
        <if test="imageData != null">
            and imageData = #{imageData}
        </if>
        <if test="insertTimeForHis != null">
            and insertTimeForHis = #{insertTimeForHis}
        </if>
        <if test="operateTimeForHis != null">
            and operateTimeForHis = #{operateTimeForHis}
        </if>
    </sql>

    <!-- 按对象查询记录的SQL部分 -->
    <sql id="Base_Select_By_Entity">
        select
        <include refid="Base_Column_List"/>
        from imagestorage
        <where>
            <include refid="Base_Select_By_Entity_Where"/>
        </where>
    </sql>

    <!-- 按主键查询一条记录 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from imagestorage
        where imageId = #{param1}
    </select>

    <!-- 按主键List查询多条记录 -->
    <select id="selectBatchByPrimaryKeys" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from imagestorage
        where imageId in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <!-- 按对象查询一页记录（多条记录） -->
    <select id="selectPage" resultMap="BaseResultMap" parameterType="com.lawfirm.lawfirmserver.image.po.ImageStorage">
        <include refid="Base_Select_By_Entity"/>
    </select>

    <!-- 按主键删除一条记录 -->
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete
        from imagestorage
        where imageId = #{param1}
    </delete>

    <!-- 按主键List删除多条记录 -->
    <delete id="deleteBatchByPrimaryKeys" parameterType="map">
        delete from imagestorage
        where imageId in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 完整插入一条记录-->
    <insert id="insert" parameterType="com.lawfirm.lawfirmserver.image.po.ImageStorage">
        insert into imagestorage (imageId, userId, imageType, fileExtension, imageData,
                                  operateTimeForHis)
        values (#{imageId}, #{userId}, #{imageType}, #{fileExtension}, #{imageData},
                now())
    </insert>

    <!-- 插入一条记录(为空的字段不操作) -->
    <insert id="insertSelective" parameterType="com.lawfirm.lawfirmserver.image.po.ImageStorage">
        insert into imagestorage
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="imageId != null">
                imageId,
            </if>
            <if test="userId != null">
                userId,
            </if>
            <if test="imageType != null">
                imageType,
            </if>
            <if test="fileExtension != null">
                fileExtension,
            </if>
            <if test="imageData != null">
                imageData,
            </if>
            operateTimeForHis,
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="imageId != null">
                #{imageId},
            </if>
            <if test="userId != null">
                #{userId},
            </if>
            <if test="imageType != null">
                #{imageType},
            </if>
            <if test="fileExtension != null">
                #{fileExtension},
            </if>
            <if test="imageData != null">
                #{imageData},
            </if>
            now(),
        </trim>
    </insert>

    <!-- 更新一条记录(为空的字段不操作) -->
    <update id="updateSelectiveByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.image.po.ImageStorage">
        update imagestorage
        <set>
            <if test="userId != null">
                userId=#{userId},
            </if>
            <if test="imageType != null">
                imageType=#{imageType},
            </if>
            <if test="fileExtension != null">
                fileExtension=#{fileExtension},
            </if>
            <if test="imageData != null">
                imageData=#{imageData},
            </if>
            operateTimeForHis=now(),
        </set>
        where imageId = #{imageId}
    </update>

    <!-- 完整更新一条记录 -->
    <update id="updateByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.image.po.ImageStorage">
        update imagestorage
        set userId=#{userId},
            imageType=#{imageType},
            fileExtension=#{fileExtension},
            imageData=#{imageData},
            operateTimeForHis=now()
        where imageId = #{imageId}
    </update>

</mapper>