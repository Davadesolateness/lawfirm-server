<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================== -->
<!-- ============================================================== -->
<!-- =======通过ins-framework-mybatis工具自动生成，请勿手工修改！======= -->
<!-- =======本配置文件中定义的节点可在自定义配置文件中直接使用！       ======= -->
<!-- ============================================================== -->
<!-- ============================================================== -->
<mapper namespace="com.lawfirm.lawfirmserver.chat.dao.ChatMessageDao">
    <!-- 通用查询结果对象-->
    <resultMap id="BaseResultMap" type="com.lawfirm.lawfirmserver.chat.po.ChatMessage">
        <id column="messageId" property="messageId"/>
        <result column="sessionId" property="sessionId"/>
        <result column="senderId" property="senderId"/>
        <result column="receiverId" property="receiverId"/>
        <result column="messageContent" property="messageContent"/>
        <result column="sendTime" property="sendTime"/>
        <result column="isRead" property="isRead"/>
        <result column="insertTimeForHis" property="insertTimeForHis"/>
        <result column="operateTimeForHis" property="operateTimeForHis"/>
    </resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        messageId
        , sessionId, senderId, receiverId, messageContent,
		 sendTime, isRead, insertTimeForHis, operateTimeForHis
    </sql>

    <!-- 按对象查询记录的WHERE部分 -->
    <sql id="Base_Select_By_Entity_Where">
        <if test="messageId != null">
            and messageId = #{messageId}
        </if>
        <if test="sessionId != null">
            and sessionId = #{sessionId}
        </if>
        <if test="senderId != null">
            and senderId = #{senderId}
        </if>
        <if test="receiverId != null">
            and receiverId = #{receiverId}
        </if>
        <if test="messageContent != null">
            and messageContent = #{messageContent}
        </if>
        <if test="sendTime != null">
            and sendTime = #{sendTime}
        </if>
        <if test="isRead != null">
            and isRead = #{isRead}
        </if>
        <if test="insertTimeForHis != null">
            and insertTimeForHis = #{insertTimeForHis}
        </if>
        <if test="operateTimeForHis != null">
            and operateTimeForHis = #{operateTimeForHis}
        </if>
    </sql>

    <!-- 按对象查询记录的SQL部分 -->
    <sql id="Base_Select_By_Entity">
        select
        <include refid="Base_Column_List"/>
        from chatmessage
        <where>
            <include refid="Base_Select_By_Entity_Where"/>
        </where>
    </sql>

    <!-- 按主键查询一条记录 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from chatmessage
        where messageId = #{param1}
    </select>

    <!-- 按主键List查询多条记录 -->
    <select id="selectBatchByPrimaryKeys" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from chatmessage
        where messageId in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <!-- 按对象查询一页记录（多条记录） -->
    <select id="selectPage" resultMap="BaseResultMap" parameterType="com.lawfirm.lawfirmserver.chat.po.ChatMessage">
        <include refid="Base_Select_By_Entity"/>
    </select>

    <!-- 按主键删除一条记录 -->
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete
        from chatmessage
        where messageId = #{param1}
    </delete>

    <!-- 按主键List删除多条记录 -->
    <delete id="deleteBatchByPrimaryKeys" parameterType="map">
        delete from chatmessage
        where messageId in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 完整插入一条记录-->
    <insert id="insert" parameterType="com.lawfirm.lawfirmserver.chat.po.ChatMessage">
        insert into chatmessage (messageId, sessionId, senderId, receiverId, messageContent,
                                 sendTime, isRead, operateTimeForHis)
        values (#{messageId}, #{sessionId}, #{senderId}, #{receiverId}, #{messageContent},
                #{sendTime}, #{isRead}, now())
    </insert>

    <!-- 插入一条记录(为空的字段不操作) -->
    <insert id="insertSelective" parameterType="com.lawfirm.lawfirmserver.chat.po.ChatMessage">
        insert into chatmessage
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="messageId != null">
                messageId,
            </if>
            <if test="sessionId != null">
                sessionId,
            </if>
            <if test="senderId != null">
                senderId,
            </if>
            <if test="receiverId != null">
                receiverId,
            </if>
            <if test="messageContent != null">
                messageContent,
            </if>
            <if test="sendTime != null">
                sendTime,
            </if>
            <if test="isRead != null">
                isRead,
            </if>
            operateTimeForHis,
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="messageId != null">
                #{messageId},
            </if>
            <if test="sessionId != null">
                #{sessionId},
            </if>
            <if test="senderId != null">
                #{senderId},
            </if>
            <if test="receiverId != null">
                #{receiverId},
            </if>
            <if test="messageContent != null">
                #{messageContent},
            </if>
            <if test="sendTime != null">
                #{sendTime},
            </if>
            <if test="isRead != null">
                #{isRead},
            </if>
            now(),
        </trim>
    </insert>

    <!-- 更新一条记录(为空的字段不操作) -->
    <update id="updateSelectiveByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.chat.po.ChatMessage">
        update chatmessage
        <set>
            <if test="sessionId != null">
                sessionId=#{sessionId},
            </if>
            <if test="senderId != null">
                senderId=#{senderId},
            </if>
            <if test="receiverId != null">
                receiverId=#{receiverId},
            </if>
            <if test="messageContent != null">
                messageContent=#{messageContent},
            </if>
            <if test="sendTime != null">
                sendTime=#{sendTime},
            </if>
            <if test="isRead != null">
                isRead=#{isRead},
            </if>
            operateTimeForHis=now(),
        </set>
        where messageId = #{messageId}
    </update>

    <!-- 完整更新一条记录 -->
    <update id="updateByPrimaryKey" parameterType="com.lawfirm.lawfirmserver.chat.po.ChatMessage">
        update chatmessage
        set sessionId=#{sessionId},
            senderId=#{senderId},
            receiverId=#{receiverId},
            messageContent=#{messageContent},
            sendTime=#{sendTime},
            isRead=#{isRead},
            operateTimeForHis=now()
        where messageId = #{messageId}
    </update>

</mapper>