<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lawfirm.lawfirmserver.order.dao.OrderDao">

    <!-- 基本结果映射 -->
    <resultMap id="BaseResultMap" type="com.lawfirm.lawfirmserver.order.po.Order">
        <id column="order_id" property="orderId"/>
        <result column="user_id" property="userId"/>
        <result column="user_type" property="userType"/>
        <result column="order_type" property="orderType"/>
        <result column="service_duration" property="serviceDuration"/>
        <result column="service_count" property="serviceCount"/>
        <result column="purchase_amount" property="purchaseAmount"/>
        <result column="order_status" property="orderStatus"/>
        <result column="input_time" property="inputTime"/>
        <result column="insert_time_for_his" property="insertTimeForHis"/>
        <result column="operate_time_for_his" property="operateTimeForHis"/>
    </resultMap>
    
    <!-- 基本列 -->
    <sql id="Base_Column_List">
        order_id, user_id, user_type, order_type, service_duration, service_count, 
        purchase_amount, order_status, input_time, insert_time_for_his, operate_time_for_his
    </sql>
    
    <!-- 根据用户ID查询订单列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM orders
        WHERE user_id = #{userId}
        ORDER BY input_time DESC
    </select>
    
    <!-- 根据订单ID查询订单 -->
    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM orders
        WHERE order_id = #{orderId}
    </select>
    
</mapper>
