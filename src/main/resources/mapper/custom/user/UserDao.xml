<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================================== -->
<!-- ================可直接使用Base配置文件中定义的节点！================ -->
<!-- ============================================================== -->
<mapper namespace="com.lawfirm.lawfirmserver.user.dao.UserDao">
    <!-- 请在下方添加自定义配置-->

    <!-- 按用户名查询一条记录 -->
    <select id="selectUserByUserName" resultMap="BaseResultMap" parameterType="map">
        select
        <include refid="Base_Column_List"/>
        from user
        where username = #{username}
    </select>

    <!-- 插入一条记录(为空的字段不操作) -->
    <insert id="insertSelectiveAndBack" parameterType="com.lawfirm.lawfirmserver.user.po.User">
        <selectKey keyProperty="id" resultType="Long" order="AFTER">
            SELECT LAST_INSERT_ID() AS value
        </selectKey>
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="username != null">
                username,
            </if>
            <if test="password != null">
                password,
            </if>
            <if test="passwordUpdateTime != null">
                password_update_time,
            </if>
            <if test="email != null">
                email,
            </if>
            <if test="phoneNumber != null">
                phone_number,
            </if>
            <if test="userType != null">
                user_type,
            </if>
            <if test="sourceType != null">
                source_type,
            </if>
            <if test="relatedEntityId != null">
                related_entity_id,
            </if>
            <if test="isValidFlag != null">
                is_valid_flag,
            </if>
            operate_time_for_his,
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
            </if>
            <if test="username != null">
                #{username},
            </if>
            <if test="password != null">
                #{password},
            </if>
            <if test="passwordUpdateTime != null">
                #{passwordUpdateTime},
            </if>
            <if test="email != null">
                #{email},
            </if>
            <if test="phoneNumber != null">
                #{phoneNumber},
            </if>
            <if test="userType != null">
                #{userType},
            </if>
            <if test="sourceType != null">
                #{sourceType},
            </if>
            <if test="relatedEntityId != null">
                #{relatedEntityId},
            </if>
            <if test="isValidFlag != null">
                #{isValidFlag},
            </if>
            now(),
        </trim>
    </insert>

</mapper>